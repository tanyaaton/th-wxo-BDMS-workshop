# Python dependencies for Inventory Query Tool (SQLite Version)
# Watson Orchestrate SDK and data processing libraries

ibm-watsonx-orchestrate>=1.0.0
pandas>=2.1.0,<3.0.0
tabulate==0.9.0

# Note: sqlite3 is built into Python, no installation needed